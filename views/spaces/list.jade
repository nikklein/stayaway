extends ../layout
block content
  h1= title

  p Thanks for adding your property you mug!
  div(class='spaces')
   each val in spaces
      div(id='space-listing')
        a(id='space-name', href = '/spaces/' + val.id)= val.name
        div(id='space-description')= val.description
        div(id='space-price')= '£' + val.price +'.00 per night'
